1. Run: `docker compose up -d`
2. Visit `https://www.coingecko.com/en/developers/dashboard` and generate **API Key**
3. Put your **API KEY** in `src/main/resources/application.properties` in api-key variable
4. Run App.java
5. Visit swagger `http://localhost:8080/swagger-ui/index.html`